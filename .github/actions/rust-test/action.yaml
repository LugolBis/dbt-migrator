name: Rust test suite

description: Run cargo test with cache and toolchain setup

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4

    - uses: dtolnay/rust-toolchain@stable

    - uses: Swatinem/rust-cache@v2

    - name: Run tests
      shell: bash
      run: cargo test --verbose